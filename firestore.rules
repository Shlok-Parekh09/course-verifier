rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read access to all documents (since this is a public dashboard)
      allow read: if true;
      // Keep write access disabled for security (backend uses admin SDK to write)
      allow write: if false;
    }
  }
}
